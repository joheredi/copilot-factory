{
  "name": "@factory/ui-components",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared React UI components — reusable components built with Tailwind and shadcn/ui.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run --project @factory/ui-components",
    "test:watch": "vitest --project @factory/ui-components",
    "test:coverage": "vitest run --project @factory/ui-components --coverage",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "lint": "eslint .",
    "format": "prettier --write ."
  }
}
