{
  "name": "@factory/schemas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Packet schemas — Zod schemas for task packets, dev results, reviews, merge, validation, and all artifact contracts.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  }
}
