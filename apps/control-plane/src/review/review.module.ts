/**
 * NestJS module for review cycle management.
 *
 * Will own controllers and services for review cycle tracking,
 * specialist review packet submission, and lead review decisions
 * once T084 is implemented.
 *
 * @module @factory/control-plane
 */
import { Module } from "@nestjs/common";

/** Feature module for review pipeline endpoints. */
@Module({})
export class ReviewModule {}
