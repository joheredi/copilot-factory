/**
 * NestJS module for policy and configuration management.
 *
 * Will own controllers and services for policy set CRUD,
 * hierarchical config resolution, and configuration snapshot
 * queries once T085 is implemented.
 *
 * @module @factory/control-plane
 */
import { Module } from "@nestjs/common";

/** Feature module for policy and configuration endpoints. */
@Module({})
export class PolicyModule {}
