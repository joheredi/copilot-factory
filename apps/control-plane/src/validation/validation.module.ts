/**
 * NestJS module for validation run management.
 *
 * Will own controllers and services for validation run tracking,
 * result recording, and validation profile management once the
 * validation pipeline tasks are implemented.
 *
 * @module @factory/control-plane
 */
import { Module } from "@nestjs/common";

/** Feature module for validation pipeline endpoints. */
@Module({})
export class ValidationModule {}
