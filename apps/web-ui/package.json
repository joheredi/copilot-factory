{
  "name": "@factory/web-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "React SPA operator dashboard — system health, task board, review center, merge queue, config editor, audit explorer.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  }
}
