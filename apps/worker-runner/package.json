{
  "name": "@factory/worker-runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Worker process supervisor — spawns, monitors, and manages ephemeral worker processes for task execution.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run --project @factory/worker-runner",
    "test:watch": "vitest --project @factory/worker-runner",
    "test:coverage": "vitest run --project @factory/worker-runner --coverage",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  }
}
